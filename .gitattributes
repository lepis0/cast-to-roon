# Force LF everywhere, regardless of platform autocrlf settings - this repo
# ships shell scripts that must run unmodified inside Linux containers, and the
# Docker build reads straight from the working tree.
* text=auto eol=lf
*.sh text eol=lf
*.xml text eol=lf
Dockerfile text eol=lf
